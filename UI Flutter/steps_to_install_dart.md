# Steps to Install Dart SDK (Theory Only)

## Theory:

1. Go to [https://dart.dev/get-dart](https://dart.dev/get-dart)
2. Choose your OS (Windows/Linux/macOS)
3. Download and extract the Dart SDK
4. Add the SDK bin path to system environment variables
5. Verify installation using `dart --version` in the terminal
