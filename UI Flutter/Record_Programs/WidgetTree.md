// Widget Tree
Theory:
Widget tree represents how UI elements (widgets) are structured hierarchically in Flutter.

MaterialApp
 └── Scaffold
      ├── AppBar
      └── Body
           └── Center
                └── Text("Hello Flutter")
